import Typography from 'typography';
import lawtonTheme from 'typography-theme-lawton';

// lawtonTheme.baseFontSize = '16px';

const typography = new Typography(lawtonTheme);

export default typography;
